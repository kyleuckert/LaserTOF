label: line tool
label: reset dragged position

generate figure
-plots label (if the label array is not empty)
-draws each label
--I could separate format, offset, precision into an array for each label
--places the label in the xy position (within the label or label_time/label_mass/label_intensity array)
-allows user to drag labels

need to retrieve new label position and update array
